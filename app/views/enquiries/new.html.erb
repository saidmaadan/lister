<h1>Make Enquiry about <%= link_to @listing.title, @listing %></h1>
</header>

<%= form_for([@listing, @enquiry]) do |f| %>
  <%= render "shared/errors", object: @enquiry %>
  <fieldset>
    <ol>
      <li class="required">
        <%= f.label :comment %>
        <%= f.text_area :comment%>
      </li>
    </ol>
    <p>
      <%= f.submit  'Make Enquiry' %> 
      <%= link_to "Cancel", @listing, class: 'button' %>
    </p
  </fieldset>
<% end %>