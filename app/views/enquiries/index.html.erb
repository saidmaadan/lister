 <h1>Enquiries for <%= link_to @listing.title, @listing %></h1>
</header>


  <% @enquiries.each do |enquiry| %>
    <p>
    <%= profile_image_for(enquiry.user, size: 35) %>
     by <%= link_to enquiry.user.name, enquiry.user %>
      <span>
        <%= time_ago_in_words(enquiry.created_at) %> ago
      </span>
      <br><%= enquiry.comment %>   
    </p>
  <% end %>
