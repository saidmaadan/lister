<div class='row'>
<div class='small-8 large-centered columns'>

<header>
  <h1><%= pluralize(@users.size, 'User') %></h1>
</header>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>   
    </li>
  <% end %>
</ul>
</div></div>